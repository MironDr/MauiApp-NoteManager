<?xml version="1.0" encoding="utf-8"?>

<view:BaseView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:view="clr-namespace:MauiApp1.View"
             x:Class="MauiApp1.Views.Notes.NoteTypeSelectorView">
    <VerticalStackLayout Padding="20" Spacing="10" x:Name="MainLayout">
        <Label Text="Выберите тип заметки" FontSize="20" />

        <CollectionView ItemsSource="{Binding NoteTypes}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding .}" 
                            Command="{Binding BindingContext.SelectNoteTypeCommand, Source={x:Reference MainLayout}}"
                            CommandParameter="{Binding .}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</view:BaseView>